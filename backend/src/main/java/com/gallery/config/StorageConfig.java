package com.gallery.config;

import org.springframework.context.annotation.Configuration;

/**
 * Configuration for storage settings.
 * Currently uses application.properties for storage paths.
 */
@Configuration
public class StorageConfig {
    // Storage configuration is handled via application.properties
    // This class can be extended with additional storage-related beans if needed
}
